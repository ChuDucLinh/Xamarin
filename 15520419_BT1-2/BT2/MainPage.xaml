<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BT2_15520456"
             x:Class="BT2_15520419.MainPage">

    <ListView x:Name="lvFlower"
              HasUnevenRows="True"
              GroupDisplayBinding="{Binding Name}"
              GroupShortNameBinding="{Binding Name}"
              IsGroupingEnabled="True">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ImageCell ImageSource="{Binding Image}" Text="{Binding Name}" Detail="{Binding Price}"></ImageCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</ContentPage>
